/**
 * Beyond Mortals — Design System
 *
 * Single entry point. Every page and component imports from here.
 *
 *   import { palette, type, layout, cn } from "@/design-system"
 */

export { palette, typeface, type, layout, motion } from "./tokens";
export { cn } from "./utils";
export { fontSerif, fontSans, fontMono, fontVariables } from "./fonts";
